I represent the list data type, that is the type of the return value of some function calls of the hunspell library. I can be converted to a collection usable in squeak.

Instance Variables
	listSize:		<Integer>
