language: smalltalk
sudo: true

matrix:
  include:
  - os: linux
    dist: bionic
  - os: linux
    dist: xenial
before_install:
  - sudo apt install libstdc++
os:
  - linux
  - windows
  #- osx

smalltalk:
  - Squeak32-5.2
